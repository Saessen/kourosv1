{% extends 'base.html.twig' %}

{% block title %}Conventions{% endblock %}

{% block body %}
    <h1>Conventions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ convention.id }}</td>
            </tr>
            <tr>
                <th>LieuFormation</th>
                <td>{{ convention.lieuFormation }}</td>
            </tr>
            <tr>
                <th>NomParticipants</th>
                <td>{{ convention.nomParticipants }}</td>
            </tr>
            <tr>
                <th>PrenomParticipants</th>
                <td>{{ convention.prenomParticipants }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('conventions_index') }}">back to list</a>

    <a href="{{ path('conventions_edit', {'id': convention.id}) }}">edit</a>

    {{ include('conventions/_delete_form.html.twig') }}
{% endblock %}
